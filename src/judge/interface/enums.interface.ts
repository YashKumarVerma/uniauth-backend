/**
 * Interface represents the structure of local language mappings
 *
 * @category Judge
 */
export interface LanguageStruct {
  /** judge0 language id */
  id: number;

  /** code extension of language */
  extension: string;
}
