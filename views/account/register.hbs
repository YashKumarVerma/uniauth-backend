<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{{app.title}}</title>
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/tailwindcss/2.0.2/tailwind.min.css">
    <link rel="icon" href="https://avatars.githubusercontent.com/u/78133145?s=200&v=4">

</head>

<body>
    {{!-- bare-bone header --}}
    <header class="sticky z-10 top-0 text-gray-700 bg-white border-t border-b body-font">
        <div class="container flex flex-col flex-wrap p-5 mx-auto md:items-center md:flex-row ">
            <a class="flex items-center w-40 font-medium text-gray-900 title-font md:mb-0">
                <h1 class="text-2xl font-semibold tracking-tighter">UniAuth</h1>
            </a>
        </div>
    </header>
    {{!-- bare bone header ends --}}

    {{!-- @todo: fix medium screen width responsiveness issue --}}
    <section class="absolute top-0 flex flex-col items-center h-screen md:flex-row ">
        <div class="hidden w-full h-screen bg-gray-400 lg:block md:w-1/3 xl:w-2/3">
            <img src="https://dummyimage.com/1000x800/F3F4F7/64748b" alt="" class="object-cover w-full h-full">
        </div>
        <div
            class="flex items-center justify-center w-full h-screen px-6 bg-blue-1300 md:max-w-md lg:max-w-full md:mx-auto md:w-1/2 xl:w-1/3 lg:px-16 xl:px-12">
            <div class="w-full h-100">
                <h1 class="mt-12 text-2xl font-semibold tracking-tighter text-blue-700 sm:text-3xl title-font">Join
                    UniAuth</h1>
                <form class="mt-6" action="" method="POST" autocomplete="off" id="regForm">
                    <input autocomplete="false" name="hidden" type="text" style="display:none;">
                    <div class="p-1">
                        <input type="text" name="name" id="name" placeholder="Full Name"
                            class="w-full px-4 py-2 mt-2 text-base text-blue-700 bg-gray-100 border-transparent rounded-lg focus:border-gray-500 focus:bg-white focus:ring-0 "
                            autofocus required>
                    </div>
                    <div class="p-1">
                        <input type="text" name="registrationNumber" id="name" placeholder="Registration Number"
                            class="w-full px-4 py-2 mt-2 text-base text-blue-700 bg-gray-100 border-transparent rounded-lg focus:border-gray-500 focus:bg-white focus:ring-0 "
                            autofocus required>
                    </div>
                    <div class="p-1">
                        <input type="text" name="collegeEmail" id="name" placeholder="College Email"
                            class="w-full px-4 py-2 mt-2 text-base text-blue-700 bg-gray-100 border-transparent rounded-lg focus:border-gray-500 focus:bg-white focus:ring-0 "
                            autofocus required>
                    </div>
                    <div class="p-1">
                        <input type="password" name="password" id="password" placeholder="Password"
                            pattern="^(?=.*[a-z])(?=.*[A-Z])(?=.*\d)(?=.*[@$!%*?&])[A-Za-z\d@$!%*?&]{8,}$"
                            title="Must contain at least one number and one uppercase and lowercase letter,and one special character ,and at least 6 or more characters"
                            class="w-full px-4 py-2 mt-2 text-base text-blue-700 bg-gray-100 border-transparent rounded-lg focus:border-gray-500 focus:bg-white focus:ring-0 "
                            autofocus required>
                        <div class="mx-4" style="float: left;padding-left:5px">
                            <label id="passwordText">show password</label>
                            <input type="checkbox" id="togglePassword"><br>
                        </div>
                    </div>
                    <input type="hidden" name="token" value="foo">
                    <p class="mt-8 text-center text-red-500">{{server.message}}</p>
                    <button type="submit" onclick="validate()"
                        class="block w-full px-4 py-3 mt-6 font-semibold text-white transition duration-500 ease-in-out transform rounded-lg bg-gradient-to-r from-blue-700 hover:from-blue-600 to-blue-600 hover:to-blue-700 focus:shadow-outline focus:outline-none">Register</button>
                </form>
                <hr class="w-full my-6 border-gray-300">
                <p class="mt-8 text-center">Already have an account? <a href="./login"
                        class="font-semibold text-blue-500 hover:text-blue-700">Log In</a></p>
            </div>
        </div>
    </section>

    <script>

        const togglePassword = document.getElementById('togglePassword');


        const showOrHidePassword = () => {
            const password = document.getElementById('password');
            if (password.type === 'password') {
                password.type = 'text';
            } else {
                password.type = 'password';
            }
        };

        togglePassword.addEventListener('change', showOrHidePassword);
    </script>
</body>

</html>